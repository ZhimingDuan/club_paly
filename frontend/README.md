# 前端运行说明（club_paly）

本目录已补齐为完整的 Vite + React + TypeScript 工程，可直接在服务器或本机执行安装/构建。

## 本机开发运行

```bash
npm install
npm run dev
```

默认访问：`http://localhost:3000/`

## 生产构建

```bash
npm install
npm run build
```

输出：`dist/`

